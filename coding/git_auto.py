# os를 조작하는 방법
# 서브 프로세스를 실행하는 방법

# 과목
# 종류
# 세트번호

# 범위 1~6까지 혹은 a,b,c
# 를 반복해서
    # git clone 을 받도록 하는
    # base_utl = 'http~~
# 현재 위치를 기준으로,
# 최상위 폴더를 제외한 모든 폴더를 순회하여 (모든 폴더는 아마 list)
    # 폴더 이름이 ".git"인 경우를 모두 삭제 -> 'rm -f'

